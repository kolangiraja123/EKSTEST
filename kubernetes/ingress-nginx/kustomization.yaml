apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Using the stable community Helm chart or manifests
  - https://raw.githubusercontent.com/kubernetes/ingress-nginx/main/deploy/static/provider/aws/deploy.yaml